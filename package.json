{
    "name": "jquery.iframe-transport.js",
    "version": "1.0.0",
    "engines": {
        "node": ">=18 <19"
    },
    "scripts": {
        "start": "cd backend/built && node index.js",
        "prebuild": "cd backend && npm install",
        "build": "cd frontend && npm run build"
    }
}
