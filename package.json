{
    "name": "biscord",
    "version": "1.0.0",
    "engines": {
        "node": ">=18 <19"
    },
    "type": "module",
    "scripts": {
        "start": "cd backend/dist && node app.js",
        "dev": "cd backend && npm run dev & cd frontend && ng serve",
        "prebuild": "cd backend && npm run build",
        "build": "cd frontend && npm run build"
    }
}
