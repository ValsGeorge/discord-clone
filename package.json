{
    "name": "biscord",
    "version": "1.0.0",
    "engines": {
        "node": ">=18 <19"
    },
    "type": "module",
    "scripts": {
        "start": "cd backend/dist && node app.js",
        "prebuild": "cd backend && npm install",
        "build": "cd frontend && npm run build"
    }
}
